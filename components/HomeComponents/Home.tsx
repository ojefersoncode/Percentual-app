'use client';

import Cards from './Cards';

export const Home = () => {
  return (
    <section className="flex flex-col bg-background dark:bg-background p-4">
      <Cards />
    </section>
  );
};
